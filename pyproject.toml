[project]
name = "qwen3.6-27b-deploy"
version = "0.1.0"
description = "Qwen3.6-27B vLLM deployment with speculative decoding benchmarks"
requires-python = ">=3.10"
dependencies = [
    "llmeter>=0.1.12",
    "openai>=2.44.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
